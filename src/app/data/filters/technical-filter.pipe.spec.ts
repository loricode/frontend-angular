import { TechnicalFilterPipe } from './technical-filter.pipe';

describe('TechnicalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TechnicalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
